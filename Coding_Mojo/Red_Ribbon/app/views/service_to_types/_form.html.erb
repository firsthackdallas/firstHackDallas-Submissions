<%= form_for(@service_to_type) do |f| %>
  <% if @service_to_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service_to_type.errors.count, "error") %> prohibited this service_to_type from being saved:</h2>

      <ul>
      <% @service_to_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :service_provider_id %><br>
    <%= f.text_field :service_provider_id %>
  </div>
  <div class="field">
    <%= f.label :service_type_id %><br>
    <%= f.text_field :service_type_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
